# Prisma / Next.js — historia batchy (osobna baza niż Temporal w tym samym Postgresie).
# Skopiuj do `.env.local` i dostosuj host, jeśli Postgres nie jest na localhost:5432.
DATABASE_URL="postgresql://temporal:temporal@localhost:5432/invoice_app"
