generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Batch {
  id         String   @id @default(cuid())
  workflowId String   @unique
  status     String
  sessionId  String?  // "demo_<uuid>" for demo tenants; null/"global" for local dev
  createdAt  DateTime @default(now())
  invoices   Invoice[]

  @@index([sessionId])
}

model Invoice {
  id             String  @id @default(cuid())
  batchId        String
  invoiceId      String
  status         String
  expectedAmount Float?
  reason         String
  batch          Batch   @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@index([batchId])
}
