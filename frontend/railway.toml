# Railway service config — Next.js frontend.
#
# In the Railway dashboard, point this service's Root Directory to "/frontend"
# so Nixpacks detects package.json and the Prisma schema in the right tree.
#
# We deliberately use Nixpacks (not Dockerfile) for the frontend because:
# - It has no Dockerfile and we don't want to maintain one just for Railway.
# - Nixpacks auto-detects Next.js 16 + npm and runs `npm ci && npm run build`.
# - `npm run build` runs `prisma generate && next build` so @prisma/client model
#   types exist during `tsc` on Railway (they are not bundled without generate).
#
# ``nixpacks.toml`` in this directory pins a fresh nixpkgs archive so
# ``NIXPACKS_NODE_VERSION=22`` resolves to a Prisma-compatible patch (>= 22.12).

[build]
builder = "NIXPACKS"
# Client generation is wired in package.json ``build`` script (``prisma generate && next build``).

[deploy]
# `npm start` runs `next start` which respects $PORT (Next.js 13+ default).
startCommand = "npm start"
# Requires ``DATABASE_URL`` (Railway Postgres reference) so Prisma schema
# matches the DB before the app boots.
# Railway allows at most one array element for preDeployCommand — use a shell string.
preDeployCommand = "npx prisma db push"

# Next.js serves a 200 at the root once boot is complete. If you add a
# dedicated /api/health route later, point this at it.
healthcheckPath = "/"
healthcheckTimeout = 100

restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
